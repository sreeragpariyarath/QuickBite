export const ROUTES = {
  LOGIN: '/login',
  DASHBOARD: '/dashboard',
  ORDERS: '/orders',
  RESTAURANTS: '/restaurants',
  USERS: '/users',
} as const;

export const LOCAL_STORAGE_KEYS = {
  ACCESS_TOKEN: 'qb_admin_token',
  USER_PROFILE: 'qb_admin_user',
} as const;
